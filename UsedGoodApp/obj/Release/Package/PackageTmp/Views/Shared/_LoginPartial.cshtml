@using Microsoft.AspNet.Identity

    @if (Request.IsAuthenticated)
    {
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "form-inline my-2 my-lg-0" }))
        {
            @Html.AntiForgeryToken()

            @*<ul class="">
                    <li>
                        Зраствуйте @User.Identity.GetUserName() !
                    </li>
                    <li>
                        @Html.ActionLink("Выйти", "Logout", "Account")
                    </li>
                </ul>*@
            <ul class="nav navbar-nav navbar-right">
                <li><a class="nav-item nav-link" href="#">Зраствуйте @User.Identity.GetUserName() !</a></li>
                <li><a class="nav-item nav-link" href="/Account/Logout">Выйти</a></li>
            </ul>                      
        }
    }
    else
    {
        <ul class="nav navbar-nav navbar-right">
            @*<li>@Html.ActionLink("Регистрация", "Register", "Account", routeValues: null, htmlAttributes: new {@class="mr-2", id = "registerLink" })</li>*@
            <li>@Html.ActionLink("Выполнить вход", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
        </ul>
    }

